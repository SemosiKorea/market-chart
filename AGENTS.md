# AGENTS.md

이 문서는 Codex 및 기타 코딩 에이전트가 이 저장소에서 작업할 때 따라야 할 기본 규칙이다.

## 1. 최우선 원칙

1. 기존 `market-chart` 기능과 배포 방식을 먼저 조사하고 보존한다.
2. 새 기능을 구현하기 전에 저장소 구조, 실행 진입점, 의존성, 배포 설정, 비밀정보 처리 방식을 문서화한다.
3. 확인하지 않은 기존 파일을 삭제하거나 대규모로 재구성하지 않는다.
4. 실제 매매 주문 기능은 구현하거나 활성화하지 않는다. 이 계획의 1차 범위는 시장데이터 수집·정규화·비교·신호 시뮬레이션이다.
5. API 키, 계좌번호, 토큰, 사용자 식별정보를 코드·로그·테스트 픽스처·커밋에 포함하지 않는다.
6. 외부 데이터의 시각을 추정하지 말고, 원본 시장시각과 프로그램 수신시각을 별도로 저장한다.
7. 데이터가 없거나 품질이 낮으면 신호를 만들지 말고 `unknown` 또는 `invalid` 상태를 명시한다.

## 2. 작업 시작 절차

모든 구현 작업은 다음 순서로 진행한다.

1. `CODEX_START_HERE.md`를 읽는다.
2. `docs/market-signal-mvp/` 아래 문서를 모두 읽는다.
3. 현재 저장소의 파일 구조와 실행 방법을 조사한다.
4. 기존 기능과 충돌 가능성을 확인한다.
5. 구현 계획과 수정 예정 파일을 먼저 제시한다.
6. 한 단계씩 작게 구현하고 단계별 테스트를 실행한다.

저장소 구조가 계획서의 가정과 다르면 기존 구조에 맞춰 조정하되, 조정 이유를 결과 보고에 남긴다.

## 3. 기술 원칙

- Python을 우선 사용하되, 기존 저장소가 다른 런타임을 사용한다면 기존 기술 스택을 우선 존중한다.
- 네트워크 I/O는 서로 독립적으로 실패할 수 있도록 공급자별 어댑터로 분리한다.
- IBKR와 KIS의 데이터 모델을 공통 `MarketQuote` 형식으로 정규화한다.
- 시간대는 내부적으로 UTC aware datetime을 사용하고, 화면 표시만 `Asia/Seoul`로 변환한다.
- 가격 계산에는 `float`보다 `Decimal` 사용을 우선 검토한다.
- SQLite를 1차 저장소로 사용하되 저장 계층을 추상화한다.
- 외부 API 호출에는 타임아웃, 재시도 상한, 지수 백오프, 연결 상태 기록을 적용한다.
- 한 공급자 장애가 다른 공급자 수집을 중단시키지 않도록 한다.

## 4. 테스트 원칙

최소한 다음 테스트를 작성한다.

- 시장시각과 수신시각 정규화
- 수익률 계산
- QQQ bid/ask 중간값과 스프레드 계산
- 오래된 호가 및 비정상 스프레드 필터링
- 임계값 최초 통과와 중복 신호 방지
- 쿨다운과 재활성화
- IBKR 지연 데이터와 KIS QQQ 데이터의 시장시각 정렬
- 연결 종료 및 재연결 상태 전이
- 비밀정보 마스킹

외부 API 통합 테스트는 환경변수가 없으면 자동으로 건너뛰어야 한다. 단위 테스트가 실제 계좌나 실시간 네트워크를 요구해서는 안 된다.

## 5. 보안 및 매매 안전

- `.env`는 커밋하지 않는다.
- `.env.example`에는 빈 값과 설명만 둔다.
- 로그에 App Key, App Secret, access token, 계좌번호 전체를 출력하지 않는다.
- 실제 주문 엔드포인트를 호출하는 코드는 1차 MVP에 추가하지 않는다.
- 향후 주문 기능이 추가되더라도 기본값은 반드시 비활성화한다.
- LLM에는 인증정보와 계좌 식별정보를 전달하지 않는다.

## 6. 코드 품질

- 공개 함수와 핵심 클래스에는 타입 힌트를 작성한다.
- 공급자별 원본 응답을 도메인 모델과 분리한다.
- 긴 함수보다 작은 순수 함수와 명시적인 상태 전이를 선호한다.
- 오류를 조용히 무시하지 말고 구조화된 상태 또는 예외로 남긴다.
- 공급자별 API 세부사항이 비교·신호 엔진으로 누출되지 않도록 한다.

## 7. 완료 보고 형식

작업 완료 시 다음을 보고한다.

- 생성·수정한 파일
- 구현된 기능
- 실행한 명령과 테스트 결과
- 실제 API로 확인한 항목과 모의 데이터로만 확인한 항목
- 남아 있는 위험과 미구현 범위
- 다음 작업으로 권장하는 하나의 단계
