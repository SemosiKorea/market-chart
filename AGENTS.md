# AGENTS.md

## 먼저 읽을 문서

1. `MARKET_SIGNAL_MVP_PLAN.md`
2. `MVP_PRODUCT_SPEC.md`
3. `MVP_ARCHITECTURE.md`
4. `MVP_TASKS.md`
5. `CODEX_FIRST_TASK.md`

## 작업 원칙

- 기존 `market-chart` 기능과 배포 방식을 먼저 조사하고 보존한다.
- 확인하지 않은 파일을 삭제하거나 대규모로 재구성하지 않는다.
- 첫 구현 전에 저장소 구조, 실행 진입점, 의존성, 배포 설정을 조사한다.
- 조사 결과와 계획 조정 사항을 `REPOSITORY_AUDIT.md`에 기록한다.
- 1차 범위는 데이터 수집, 정규화, 저장, 비교, 연구용 신호이다.
- 실제 주문, 자동매매, 뉴스 수집, LLM 리포트는 구현하지 않는다.
- API 키, 계좌번호, 토큰을 코드, 로그, 테스트, 커밋에 포함하지 않는다.

## 기술 원칙

- 기존 기술 스택을 우선 존중한다.
- IBKR와 KIS 연결부를 독립된 공급자 어댑터로 분리한다.
- 공통 `MarketQuote` 모델을 사용한다.
- 시장시각과 프로그램 수신시각을 별도로 저장한다.
- 내부 시간은 UTC aware datetime을 사용하고 화면만 Asia/Seoul로 변환한다.
- 한 공급자의 장애가 다른 공급자 수집을 중단시키지 않도록 한다.
- 데이터가 오래됐거나 품질이 낮으면 신호를 생성하지 않는다.

## 테스트

최소 테스트 항목:

- 시간 정규화
- 수익률 계산
- QQQ bid/ask 중간값과 스프레드
- 오래된 호가와 비정상 스프레드 필터
- 임계값 최초 통과와 중복 방지
- 쿨다운과 재활성화
- 비밀정보 마스킹

외부 자격증명이 없으면 통합 테스트는 자동으로 건너뛰고, 단위 테스트는 실제 네트워크나 계좌를 요구하지 않아야 한다.

## 완료 보고

- 생성·수정 파일
- 구현 기능
- 실행한 테스트와 결과
- 실제 API 검증 여부
- 남은 위험과 미구현 범위
- 다음 권장 작업 한 단계
